export default function AdminTestPage() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'monospace' }}>
      <h1>Admin routes are deployed</h1>
      <p>Build timestamp: {new Date().toISOString()}</p>
    </div>
  );
}
